name: smartmenu_app
description: Menu interactif pour restaurants
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.3
  mobile_scanner: ^3.5.7

  # Navigation et état
  go_router: ^12.0.0
  provider: ^6.0.5

  # UI et design
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.7
  cached_network_image: ^3.3.0

  # QR Code
  # qr_code_scanner: ^1.0.1
  qr_flutter: ^4.1.0

  # Stockage local
  shared_preferences: ^2.2.2

  # HTTP et API
  http: ^1.1.0

  # Internationalisation
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  firebase_auth: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
